# 强制与远程 develop 保持一致
git fetch origin
git reset --hard origin/develop
or   git reset --hard origin/main
or   git switch <远程分支名>

# 切换到已存在的本地分支
git switch <分支名>
git checkout <分支名>

# 创建并直接切换到新分支
git switch -c <新分支名>
git checkout -b <新分支名>
